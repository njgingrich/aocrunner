import dayTemplate from './day.ts.ts';
import denoJsonTemplate from './deno.json.ts';

const TEMPLATE_MAP = {
  "template/main.ts": dayTemplate,
  "deno.json": denoJsonTemplate,
};

export {TEMPLATE_MAP};
